# xai-assignment

1. Package Installation
Running the commands listed below should download and install all neccessary packages to run the provided code.
    pip install -r requirements.txt
    python -m spacy download en_core_web_sm

2. Usage
None of the programming logic is embedded in functions so running the main.py file should execute the script as intended.